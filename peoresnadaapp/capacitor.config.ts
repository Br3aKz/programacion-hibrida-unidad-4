import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.peoresnadaapp',
  appName: 'peoresnadaapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
